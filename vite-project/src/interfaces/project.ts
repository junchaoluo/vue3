export interface SearchProjectParams {
    keywords: string,
    pageIndex: number,
    pageSize: number
}

export interface SearchProductListParams {
    pageNum: number,
    pageSize: number,
    pdNo: string
}